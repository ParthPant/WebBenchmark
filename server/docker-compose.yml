version: "3.9"
services:
  server:
    privileged: true
    build: 
      context: .
      dockerfile: Dockerfile.server
    ports: 
      - 8080:8080
    depends_on:
      - "redis"

  redis:
    build: 
      context: .
      dockerfile: Dockerfile.redis
